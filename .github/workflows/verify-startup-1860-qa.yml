name: PR 3231 QA
on:
  push:
    branches: [verify/startup-1860-qa]
permissions:
  contents: read
concurrency:
  group: startup-1860-qa
  cancel-in-progress: true
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - name: Archive inspected sources
        run: git archive --format=tar.gz --output=pr3231-sources.tar.gz HEAD
      - uses: actions/upload-artifact@v4
        with:
          name: pr3231-sources
          path: pr3231-sources.tar.gz
          retention-days: 7
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven
      - name: Install SWT browser runtime
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y libwebkit2gtk-4.1-0
      - name: Verify baseline
        run: |
          set -o pipefail
          xvfb-run -a mvn -B -U -e verify -Pbuild-individual-bundles \
            -pl org.eclipse.jdt.ui.tests -am -Dtest=PackageExplorerTests \
            -DfailIfNoTests=false -Dsurefire.failIfNoSpecifiedTests=false \
            -Dcompare-version-with-baselines.skip=true -DskipTests=false | tee qa-baseline.log
      - name: Collect reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pr3231-qa-reports
          include-hidden-files: true
          path: |
            qa-*.log
            **/target/surefire-reports/**
            **/target/work/data/.metadata/*.log
          retention-days: 7
