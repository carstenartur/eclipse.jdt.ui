name: PR 3163 targeted serialization tests

on:
  push:
    branches:
      - debug/pr3163-jenkins

jobs:
  serialization:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '25'
          cache: maven
      - uses: stCarolas/setup-maven@d6af6abeda15e98926a57b5aa970a96bb37f97d1
        with:
          maven-version: 3.9.9
      - name: Run serialization test classes
        run: |
          xvfb-run -a mvn -U -e --batch-mode \
            -pl org.eclipse.jdt.ui.tests -am verify \
            -DskipTests=false \
            -Dtest=org.eclipse.jdt.junit.tests.TestRunSessionSerializationTests3,org.eclipse.jdt.junit.tests.TestRunSessionSerializationTests4 \
            -DfailIfNoTests=false \
            -Pbree-libs -Pbuild-individual-bundles
